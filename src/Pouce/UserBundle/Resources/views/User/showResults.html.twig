<p>
    Vous êtes arrivés jusque : {{result.position.city.name}}, {{result.position.city.country.name}}<br/>
    {% if (result.position.lateness != 0) %}
    	Vous êtes arrivée avec {{result.position.lateness}}h de retard
    {% else %}
    	Vous n'avez pas eu de retard
    {% endif %}<br/>

<p>
